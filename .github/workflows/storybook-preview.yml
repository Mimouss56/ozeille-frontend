name: Storybook Preview

on:
  pull_request:
    branches: [develop, main]
    types: [opened, synchronize, reopened, closed]
    paths:
      - "src/**"
      - ".storybook/**"
      - "package.json"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: storybook-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  storybook-check-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # ------------------------------------------------------------------
      # 🛑 OPTIMISATION : Si la PR est fermée, on saute l'installation et l'audit
      # ------------------------------------------------------------------

      - name: Setup Node
        if: github.event.action != 'closed'
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        if: github.event.action != 'closed'
        run: npm ci

      # -----------------------------------------------------------
      # 🔍 PARTIE 1 : Audit (Uniquement si PR active)
      # -----------------------------------------------------------
      - name: Check for stories and deletions
        if: github.event.action != 'closed'
        id: check_stories
        run: |
          set -euo pipefail

          echo "🔍 Audit Storybook : vérification stricte des stories"

          BASE_REF="origin/${{ github.base_ref }}"
          git fetch origin "${{ github.base_ref }}" --no-tags 2>/dev/null || true

          # Récupérer tous les fichiers modifiés avec leur statut
          ALL_CHANGED=$(git diff --name-status "$BASE_REF...HEAD" 2>/dev/null || true)

          echo "📋 Fichiers modifiés détectés :"
          echo "$ALL_CHANGED" | head -20 || true

          declare -A COMPONENTS_OK
          declare -A COMPONENTS_MISSING
          declare -A COMPONENTS_DELETED

          while IFS=$'\t' read -r status filepath; do
            # Ne traiter que les fichiers src (composants, pages, layouts)
            if [[ ! "$filepath" =~ ^src/(components|pages|layouts)/ ]]; then
              continue
            fi
            
            # Ignorer les fichiers de test/story
            if [[ "$filepath" =~ \.(stories|test|spec)\. ]]; then
              continue
            fi
            
            # ⚠️ IMPORTANT : Ne vérifier QUE les fichiers .tsx (composants visuels)
            # Ignorer les fichiers .ts (hooks, utilitaires, types)
            if [[ ! "$filepath" =~ \.tsx$ ]]; then
            if [[ "$status" == A* || "$status" == M* ]]; then
              # Vérifier si la story existe
              if [[ -f "$DIR/$COMP_NAME.stories.tsx" || -f "$DIR/$COMP_NAME.stories.ts" ]]; then
                echo "✅ Couvert: $COMP_NAME"
                COMPONENTS_OK[$COMP_NAME]=1
              else
                echo "❌ MANQUANT: $COMP_NAME (pas de story trouvée)"
                COMPONENTS_MISSING[$COMP_NAME]="$DIR/$COMP_NAME.stories.tsx"
              fi
            fi
          done <<< "$ALL_CHANGED"

          # === GÉNÉRATION DU RAPPORT ===
          COMMENT_BODY="## 📚 Storybook Audit\n\n"

          # Stories manquantes - ERREUR BLOQUANTE
          MISSING_COUNT=${#COMPONENTS_MISSING[@]:-0}
          DELETED_COUNT=${#COMPONENTS_DELETED[@]:-0}
          OK_COUNT=${#COMPONENTS_OK[@]:-0}

          if [[ $MISSING_COUNT -gt 0 ]]; then
            echo ""
            echo "🚫 ERREUR : Les stories suivantes sont MANQUANTES :"
            COMMENT_BODY="$COMMENT_BODY**🚨 STORIES MANQUANTES** (Bloquant)\n\n"
            for comp in "${!COMPONENTS_MISSING[@]}"; do
              echo "  - 📄 $comp → ${COMPONENTS_MISSING[$comp]}"
              COMMENT_BODY="$COMMENT_BODY- ❌ \`$comp\`\n"
            done
            COMMENT_BODY="$COMMENT_BODY\n"
            echo ""
          fi

          # Composants supprimés
          if [[ $DELETED_COUNT -gt 0 ]]; then
            COMMENT_BODY="$COMMENT_BODY**🗑️ Composants Supprimés**\n\n"
            for comp in "${!COMPONENTS_DELETED[@]}"; do
              echo "Supprimé: $comp"
              COMMENT_BODY="$COMMENT_BODY- ~\`$comp\`~\n"
            done
            COMMENT_BODY="$COMMENT_BODY\n"
          fi

          # Composants vérifiés
          if [[ $OK_COUNT -gt 0 ]]; then
            COMMENT_BODY="$COMMENT_BODY**✅ Stories Valides** ($OK_COUNT composants)\n\n"
            for comp in $(printf '%s\n' "${!COMPONENTS_OK[@]}" 2>/dev/null | sort || true); do
              COMMENT_BODY="$COMMENT_BODY- ✅ \`$comp\`\n"
            done
            COMMENT_BODY="$COMMENT_BODY\n"
          fi

          # Aucun changement
          if [[ $OK_COUNT -eq 0 && $DELETED_COUNT -eq 0 && $MISSING_COUNT -eq 0 ]]; then
            COMMENT_BODY="$COMMENT_BODY*Aucun changement détecté sur les composants visuels*\n"
          fi

          echo "comment_body<<EOF" >> "$GITHUB_OUTPUT"
          echo -e "$COMMENT_BODY" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          # === VÉRIFICATION STRICTE : Faire échouer si des stories manquent ===
          if [[ $MISSING_COUNT -gt 0 ]]; then
            echo ""
            echo "❌ ERREUR FATALE : $MISSING_COUNT story(s) manquante(s)"
          echo "✅ Vérification réussie"

      # -----------------------------------------------------------
      # 💬 PARTIE 2 : Publication du Rapport (Uniquement si PR active)
      # -----------------------------------------------------------
      - name: Comment Audit Results
        if: github.event.action != 'closed'
        uses: actions/github-script@v7
        with:
          script: |
            const body = ${{ toJSON(steps.check_stories.outputs.comment_body) }};
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const botComment = comments.find(c => c.body.includes('📚 Storybook Audit'));
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      # -----------------------------------------------------------
      # 🚀 PARTIE 3 : Build (Uniquement si PR active)
      # -----------------------------------------------------------
      - name: Build Storybook
        if: github.event.action != 'closed'
        run: npm run build-storybook

      # -----------------------------------------------------------
      # 🧹 PARTIE 4 : Deploy OU Cleanup (Géré automatiquement par l'action)
      # -----------------------------------------------------------
      - name: Deploy or Cleanup Preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./storybook-static
          preview-branch: gh-pages-pr-previews
          umbrella-dir: pr-preview
          action: auto
          # "auto" détecte tout seul :
          # - Si PR Open/Sync -> Déploie
          # - Si PR Closed -> Supprime le dossier correspondant
